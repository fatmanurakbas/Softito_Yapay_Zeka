# 04 — Değerlendirme

Bu betik, `expected,predicted` sütunları olan bir CSV üzerinden accuracy, sınıf bazlı precision/recall ve macro F1 hesaplar. Eğitim betiğinin test metriklerine ek olarak hata analizinde kullanılabilir.

```bash
python evaluate.py --predictions predictions.csv
```
